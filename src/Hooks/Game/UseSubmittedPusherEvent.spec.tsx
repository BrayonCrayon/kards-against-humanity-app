describe("UseSubmittedPusherEvent", () => {
  // it.todo("begins with the users not having cards submitted");
  // it.todo("updates the users with submitted cards after receiving a pusher event");

  it.todo("listens for events on the pusher service");
  it.todo("updates the users submitted state");
});
